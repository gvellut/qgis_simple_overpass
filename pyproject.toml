[project]
name = "qgis-simple-overpass"
version = "0.2.0"
description = "QGIS plugin to query the Overpass API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=9.0.2"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
