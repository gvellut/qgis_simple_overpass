When closing the panel for the plugin, clear the point band def clear_clicked_point(self) -> None.

The plugin icon appears both on the web toolbar and the plugin toolbar. Only show on the Web toolbar.